# Tipo  Banco de Dados   Usuário       Endereço           Método
# Permitir conexões locais
local   all             all                                 scram-sha-256
host    all             all             0.0.0.0/0           scram-sha-256

# Permitir replicação do headquarters
host    replication     replicator     172.0.0.0/16       scram-sha-256
host    replication     replicatorB    172.0.0.0/16       scram-sha-256